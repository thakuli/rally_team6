class Avarage():
    def __init__(self):
        self.values = []

    def add(self, value):
        self.values.append()
        if self.values.append() is 5:
            self.values.pop()

    def get_avararage(self):

        if self.values.count() is 4:
            sum = sum(self.values)
            return sum / len(self.values)

        else:
            return 1
